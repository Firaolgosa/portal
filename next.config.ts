import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

  // module.exports = {
  eslint: {
    ignoreDuringBuilds: true,  // Skips linting during `next build`
  // },
}

};

export default nextConfig;
